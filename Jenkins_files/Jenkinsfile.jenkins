pipeline {
    agent any
    stages {
        stage('Clone feature branche') {
            steps {
                sh 'git clone https://github.com/Threadripper1/Large-pipe.git'
            }
        }
    }
}
